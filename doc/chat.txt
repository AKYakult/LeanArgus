这个是我目前的项目进度，然后另外一个是完整的参考项目，我希望你叫我下一步该怎么做，我不太熟悉java 和 spring，希望每一步你都能告诉我为什么这么做
同时请注意，文件内容和包格式需要严格对应参考项目，如果有你觉得更好的改动，那么你可以告诉我

我把两个项目都解压看了一遍，说说我的判断。

现状对比

你的 MyArgus 目前只搭了三个模块：auth（认证）、common（公共基础设施）、user（用户，只有 entity/mapper，业务层还没写），外加一个占位的 HealthController。

参考项目 ArgusBack 完整得多，一共 9 个模块：auth、common、user、document（文档管理）、ingestion（文档解析入库）、engine（ES/向量库/对象存储适配层）、qa（RAG 问答核心）、group（群组协作）、assistant（AI 助手对话）、metrics（LLM 用量统计）。

这是一个 RAG 知识库问答系统：用户上传文档 → 解析切片 → 向量化存入 pgvector/ES → 提问时检索相关片段 → 调用大模型生成带引用的回答。auth/user/group 是权限体系，assistant 是更高级的多轮对话 Agent。模块之间有依赖顺序，不能乱插队，这点很关键，下面会讲。

两个需要你先拿主意的问题
Spring Boot 版本不一致：你用的是 4.1.1，参考项目是 3.5.0。你已经很细心地把 mybatis-plus-spring-boot3-starter 换成了 mybatis-plus-spring-boot4-starter，说明你是故意选新版本的。这个选择本身没问题，Boot 4 是新版但对基础 Web/JPA/MyBatis 场景影响不大；只是后面照抄参考代码时，如果遇到某个类在新版里改了包名或被废弃，需要临时查一下，我会在遇到时提醒你，不用现在纠结。
包名 com.example.myargus vs com.argus.rag：我理解你说的"包格式严格对应"，指的是每个模块内部的分层结构（controller/service/mapper/model/dto/entity/vo 这一套目录约定），而不是根包名本身。根包名只是一个命名空间标识，改不改都不影响功能。我的建议是保留 com.example.myargus，不要重命名——重命名会牵连所有 import、MyBatis XML 的 namespace、以及 IDE 索引，收益却几乎为零，纯粹增加出错概率。如果你坚持要和参考项目完全一致，告诉我，我们再统一改。
接下来做什么

原则：按模块的依赖顺序推进，而不是想到哪写到哪。 后面的 document/qa/assistant 全部依赖"我是谁、我有没有权限"，所以 auth 模块必须先跑通，这也是你已经开了个头、最接近完成的部分。之后的顺序建议是：

auth（认证，进行中）→ user（用户管理）→ document + engine（文档与存储）
→ ingestion（解析入库）→ qa（问答核心）→ group（协作）
→ metrics / assistant（增强功能，最后做）
立刻要做的：把 auth 模块补完

对比下来，你已经写好了：AuthProperties（配置）、AuthConfiguration（密码哈希 Bean）、JwtAccessTokenService（只有签发 token，缺解析/校验）、UserRefreshTokenMapper、密码策略校验。这部分质量不错，逻辑和参考项目基本一致。

还缺这几块，按这个顺序补，每一步为什么这样做我写在后面：

顺序	要写的文件	为什么是这个顺序
1	JwtAccessTokenService.parse()	你只能"发"token 还不能"验"token，登录流程闭环不了
2	RefreshTokenService + RefreshTokenRecord	access token 有效期短（30分钟），得有个机制让用户不用频繁重新输密码
3	AuthService（编排注册/登录逻辑）	把 PasswordHasher、JwtAccessTokenService、RefreshTokenService 串起来
4	AuthCookieSupport	refresh token 要放进 HttpOnly Cookie，而不是像 access token 一样明文传
5	JwtAuthenticationFilter	每个请求进来时，从请求头里把 token 解出来，告诉后面的代码"这个请求是谁发的"
6	AuthController	真正对外暴露 /api/auth/login、/register、/refresh、/logout 接口
7	CurrentUserService + DevAdminInitializer	前者给其他模块提供"当前登录用户是谁"的查询入口；后者是开发环境启动时自动建一个管理员账号，省得你每次手动建

先做第 1 步，我们现场写一下，顺便讲清楚 JWT 的验证逻辑，你后面几步会用到同样的套路。

你现在的 issueToken 只做了"签发"：把用户信息塞进 JWT 的 claims 里，用密钥签名。但服务端收到一个 token 时，还需要反向操作——验证签名没被篡改、没过期、issuer 对不对，然后把 claims 里的信息取出来还原成 Java 对象。这就是 parse() 要做的事：

java
public AccessTokenClaims parse(String token) {
    try {
        Claims claims = Jwts.parser()
                .verifyWith(signingKey)      // 用签发时同一把密钥验证签名，防止 token 被伪造
                .build()
                .parseSignedClaims(token)
                .getPayload();

        validateClaims(claims);              // 检查 issuer、必需字段是否齐全

        return new AccessTokenClaims(
                claims.get(USER_ID_CLAIM, Long.class),
                claims.getSubject(),
                claims.get(DISPLAY_NAME_CLAIM, String.class),
                SystemRole.valueOf(claims.get(SYSTEM_ROLE_CLAIM, String.class)),
                claims.get(MUST_CHANGE_PASSWORD_CLAIM, Boolean.class),
                claims.getIssuedAt().toInstant(),
                claims.getExpiration().toInstant()
        );
    } catch (BusinessException exception) {
        throw exception;
    } catch (JwtException | IllegalArgumentException exception) {
        // JWT 库自己会在过期、签名不对时抛这类异常，统一包装成业务异常，
        // 不要把底层技术细节（比如具体是哪个字节校验失败）暴露给前端
        throw new BusinessException("access token 非法或已过期", exception);
    }
}

/** 校验 claims 完整性——防止拿到一个签名合法但字段缺失的畸形 token */
private void validateClaims(Claims claims) {
    if (!authProperties.getIssuer().equals(claims.getIssuer())
            || claims.getSubject() == null
            || claims.get(USER_ID_CLAIM, Long.class) == null
            || claims.get(DISPLAY_NAME_CLAIM, String.class) == null
            || claims.get(SYSTEM_ROLE_CLAIM, String.class) == null
            || claims.get(MUST_CHANGE_PASSWORD_CLAIM, Boolean.class) == null
            || claims.getIssuedAt() == null
            || claims.getExpiration() == null) {
        throw new BusinessException("access token 非法或已过期");
    }
}

AccessTokenClaims 这个 record 你已经定义了，直接复用即可，不用改。import io.jsonwebtoken.Claims; 和 io.jsonwebtoken.JwtException; 记得加上。

这一步做完后，把结果贴给我或者直接说"下一步"，我们接着做第 2 步——RefreshTokenService，那里会讲到为什么 refresh token 要存数据库而不是也用 JWT 自包含的方式（提示：因为需要支持"登出即刻失效"，纯 JWT 做不到这一点）。

package com.example.myargus.auth.security;

import com.example.myargus.auth.config.AuthProperties;
import com.example.myargus.common.enums.SystemRole;
import io.jsonwebtoken.Jwts;
import io.jsonwebtoken.security.Keys;
import org.springframework.stereotype.Service;

import javax.crypto.SecretKey;
import java.nio.charset.StandardCharsets;
import java.time.Clock;
import java.time.Instant;
import java.time.temporal.ChronoUnit;
import java.util.Date;

@Service
public class JwtAccessTokenService {
    private static final String USER_ID_CLAIM = "uid";
    private static final String DISPLAY_NAME_CLAIM = "displayName";
    private static final String SYSTEM_ROLE_CLAIM = "systemRole";
    private static final String MUST_CHANGE_PASSWORD_CLAIM = "mustChangePassword";
    private static final int MIN_SECRET_LENGTH = 32;

    private final AuthProperties authProperties;
    private final Clock clock;
    private final SecretKey signingKey;

    /** 从配置的字符串构建 HMAC 密钥 */
    private SecretKey buildSigningKey(String jwtSecret) {
        byte[] secretBytes = jwtSecret.getBytes(StandardCharsets.UTF_8);
        if (secretBytes.length < MIN_SECRET_LENGTH) {
            throw new IllegalStateException("JWT secret 至少需要 32 字节");
        }
        return Keys.hmacShaKeyFor(secretBytes);
    }

    public  JwtAccessTokenService(AuthProperties authProperties, Clock clock) {
        this.authProperties = authProperties;
        this.clock = clock;
        this.signingKey = buildSigningKey(authProperties.getJwtSecret());
    }

    /** 签发 token 时的主题信息 */
    public record TokenSubject(
            Long userId,
            String userCode,
            String displayName,
            SystemRole systemRole,
            boolean mustChangePassword
    ) {
    }

    /** 解析 token 后的声明信息 */
    public record AccessTokenClaims(
            Long userId,
            String userCode,
            String displayName,
            SystemRole systemRole,
            boolean mustChangePassword,
            Instant issuedAt,
            Instant expiresAt
    ) {
    }

    public String issueToken(TokenSubject subject) {
        Instant issuedAt = clock.instant();
        Instant expireAt = issuedAt.plus(authProperties.getAccessTokenExpireMinutes(), ChronoUnit.MINUTES);
        return Jwts.builder()
                .issuer(authProperties.getIssuer())
                .subject(subject.userCode)
                .issuedAt(Date.from(issuedAt))
                .expiration(Date.from(expireAt))
                .claim(USER_ID_CLAIM,subject.userId)
                .claim(DISPLAY_NAME_CLAIM, subject.displayName())
                .claim(SYSTEM_ROLE_CLAIM, subject.systemRole().name())
                .claim(MUST_CHANGE_PASSWORD_CLAIM, subject.mustChangePassword())
                .signWith(signingKey)
                .compact();
    }

}

在这里写吗？给我完整的

对，就加在这个类里面——issueToken 和 TokenSubject/AccessTokenClaims 之间或后面都行，我把它放在 issueToken 后面，逻辑上"先签发、后解析"顺序更好读。下面是完整文件，可以直接整个替换你现在的内容：

java
package com.example.myargus.auth.security;

import com.example.myargus.auth.config.AuthProperties;
import com.example.myargus.common.enums.SystemRole;
import com.example.myargus.common.exception.BusinessException;
import io.jsonwebtoken.Claims;
import io.jsonwebtoken.JwtException;
import io.jsonwebtoken.Jwts;
import io.jsonwebtoken.security.Keys;
import org.springframework.stereotype.Service;

import javax.crypto.SecretKey;
import java.nio.charset.StandardCharsets;
import java.time.Clock;
import java.time.Instant;
import java.time.temporal.ChronoUnit;
import java.util.Date;

@Service
public class JwtAccessTokenService {
    private static final String USER_ID_CLAIM = "uid";
    private static final String DISPLAY_NAME_CLAIM = "displayName";
    private static final String SYSTEM_ROLE_CLAIM = "systemRole";
    private static final String MUST_CHANGE_PASSWORD_CLAIM = "mustChangePassword";
    private static final int MIN_SECRET_LENGTH = 32;

    private final AuthProperties authProperties;
    private final Clock clock;
    private final SecretKey signingKey;

    /** 从配置的字符串构建 HMAC 密钥 */
    private SecretKey buildSigningKey(String jwtSecret) {
        byte[] secretBytes = jwtSecret.getBytes(StandardCharsets.UTF_8);
        if (secretBytes.length < MIN_SECRET_LENGTH) {
            throw new IllegalStateException("JWT secret 至少需要 32 字节");
        }
        return Keys.hmacShaKeyFor(secretBytes);
    }

    public JwtAccessTokenService(AuthProperties authProperties, Clock clock) {
        this.authProperties = authProperties;
        this.clock = clock;
        this.signingKey = buildSigningKey(authProperties.getJwtSecret());
    }

    /** 签发 token 时的主题信息 */
    public record TokenSubject(
            Long userId,
            String userCode,
            String displayName,
            SystemRole systemRole,
            boolean mustChangePassword
    ) {
    }

    /** 解析 token 后的声明信息 */
    public record AccessTokenClaims(
            Long userId,
            String userCode,
            String displayName,
            SystemRole systemRole,
            boolean mustChangePassword,
            Instant issuedAt,
            Instant expiresAt
    ) {
    }

    public String issueToken(TokenSubject subject) {
        Instant issuedAt = clock.instant();
        Instant expireAt = issuedAt.plus(authProperties.getAccessTokenExpireMinutes(), ChronoUnit.MINUTES);
        return Jwts.builder()
                .issuer(authProperties.getIssuer())
                .subject(subject.userCode)
                .issuedAt(Date.from(issuedAt))
                .expiration(Date.from(expireAt))
                .claim(USER_ID_CLAIM, subject.userId)
                .claim(DISPLAY_NAME_CLAIM, subject.displayName())
                .claim(SYSTEM_ROLE_CLAIM, subject.systemRole().name())
                .claim(MUST_CHANGE_PASSWORD_CLAIM, subject.mustChangePassword())
                .signWith(signingKey)
                .compact();
    }

    /**
     * 解析并校验 access token。
     * <p>
     * 会校验签名（防篡改）、过期时间（Jwts 库内部自动检查）、issuer 是否匹配，
     * 以及自定义 claims 是否齐全。任何一项不满足都统一抛 BusinessException，
     * 不把 JWT 库的底层异常细节暴露给调用方。
     */
    public AccessTokenClaims parse(String token) {
        try {
            Claims claims = Jwts.parser()
                    .verifyWith(signingKey)
                    .build()
                    .parseSignedClaims(token)
                    .getPayload();

            validateClaims(claims);

            return new AccessTokenClaims(
                    claims.get(USER_ID_CLAIM, Long.class),
                    claims.getSubject(),
                    claims.get(DISPLAY_NAME_CLAIM, String.class),
                    SystemRole.valueOf(claims.get(SYSTEM_ROLE_CLAIM, String.class)),
                    claims.get(MUST_CHANGE_PASSWORD_CLAIM, Boolean.class),
                    claims.getIssuedAt().toInstant(),
                    claims.getExpiration().toInstant()
            );
        } catch (BusinessException exception) {
            throw exception;
        } catch (JwtException | IllegalArgumentException exception) {
            throw new BusinessException("access token 非法或已过期", exception);
        }
    }

    /** 校验 claims 完整性——防止拿到一个签名合法但字段缺失的畸形 token */
    private void validateClaims(Claims claims) {
        if (!authProperties.getIssuer().equals(claims.getIssuer())
                || claims.getSubject() == null
                || claims.get(USER_ID_CLAIM, Long.class) == null
                || claims.get(DISPLAY_NAME_CLAIM, String.class) == null
                || claims.get(SYSTEM_ROLE_CLAIM, String.class) == null
                || claims.get(MUST_CHANGE_PASSWORD_CLAIM, Boolean.class) == null
                || claims.getIssuedAt() == null
                || claims.getExpiration() == null) {
            throw new BusinessException("access token 非法或已过期");
        }
    }
}

改动点说明：

新增了 import 的 Claims、JwtException、BusinessException——BusinessException 你在 common.exception 包里已经有了，直接复用。
Jwts.parser()...verifyWith(signingKey) 里的 verifyWith 是 jjwt 0.12.x 的新写法（对应 0.11.x 时代的 setSigningKey），你的 build.gradle 里锁的就是 0.12.6，版本对得上，不用改依赖。
过期校验是库自动做的：如果 token 已过期，parseSignedClaims 内部会直接抛 ExpiredJwtException（它是 JwtException 的子类），会被 catch 块捕获，你不需要自己再手动比较时间。

这一步编译应该能直接过。确认没问题的话，我们继续第 2 步——RefreshTokenService。

很好，继续。第 2 步是 RefreshTokenService，这是 auth 模块里逻辑最绕的一块，我先讲清楚设计思路，再给代码。

为什么需要单独的 Refresh Token 机制

Access token（也就是 JWT）设计成短命的（30 分钟），是为了万一泄露，损失可控——过期了自动失效。但用户不可能 30 分钟就得重新登录一次，所以需要一个"续期凭证"：refresh token，有效期长（14 天），专门用来换新的 access token。

问题是：JWT 本身是自包含、无状态的，签发后服务端不存它，就没法在用户"登出"时让它作废（除非等它自然过期）。Refresh token 反其道而行——把它存进数据库，这样就能做到：

登出时把数据库记录标记为已吊销（revoked_at），token 立刻失效；
检测到 token 被盗用重放时，也能主动吊销。
token 格式为什么是 tokenId.secret

存数据库就要能查，但如果直接把 refresh token 明文存库，一旦数据库泄露，所有 token 全部沦陷（和明文存密码一个道理）。所以做法和存密码一样——哈希后存。但哈希之后没法用来做 WHERE 查询（哈希是单向的，不能反查），所以把 token 拆成两段：

tokenId：明文存库，只用来当索引，查出候选记录；
secret：不存明文，只存它的 BCrypt 哈希，查到候选记录后再用 passwordHasher.matches() 比对。

这样即使数据库泄露，攻击者拿到的也只是哈希值，伪造不出能通过校验的 token。

两个文件

RefreshTokenRecord.java（放在 auth/service 包，和 PasswordHasher、PasswordPolicyValidator 同级）：

java
package com.example.myargus.auth.service;

import java.time.LocalDateTime;

/**
 * Refresh Token 数据库记录。
 */
public record RefreshTokenRecord(
        /** 主键 */
        Long id,
        /** 关联用户 ID */
        Long userId,
        /** token 唯一标识 */
        String tokenId,
        /** BCrypt 哈希后的 token */
        String tokenHash,
        /** 过期时间 */
        LocalDateTime expiresAt,
        /** 吊销时间，null 表示未吊销 */
        LocalDateTime revokedAt
) {

    /** 判断 token 是否当前有效 */
    public boolean isActive(LocalDateTime now) {
        return revokedAt == null && expiresAt.isAfter(now);
    }
}

这是个纯数据载体，之所以不直接把 UserRefreshToken 实体（MyBatis-Plus 的 @TableName 那个类）传来传去，是为了让 service 层不依赖持久化层的具体实现细节——这是分层的常见做法，AuthService 以后只需要认识 RefreshTokenRecord，不需要知道底层是 MyBatis 还是别的什么。

RefreshTokenService.java（放在 auth/security 包，和 JwtAccessTokenService 同级）：

java
package com.example.myargus.auth.security;

import com.example.myargus.auth.config.AuthProperties;
import com.example.myargus.auth.mapper.UserRefreshTokenMapper;
import com.example.myargus.auth.model.entity.UserRefreshToken;
import com.example.myargus.auth.service.PasswordHasher;
import com.example.myargus.auth.service.RefreshTokenRecord;
import com.baomidou.mybatisplus.core.conditions.query.LambdaQueryWrapper;
import org.springframework.stereotype.Service;

import java.security.SecureRandom;
import java.time.Clock;
import java.time.LocalDateTime;
import java.util.Base64;
import java.util.Optional;
import java.util.UUID;

/**
 * Refresh Token 管理服务。
 * <p>
 * Refresh token 格式为 {@code tokenId.secret}，tokenId 明文字段索引查找记录，
 * secret 部分 bcrypt 哈希存储。签发和吊销均操作 user_refresh_tokens 表。
 */
@Service
public class RefreshTokenService {

    private static final String TOKEN_SEPARATOR = ".";
    /** 随机 secret 字节数，Base64URL 编码后约 32 字符 */
    private static final int SECRET_BYTES = 24;

    private final UserRefreshTokenMapper tokenMapper;
    private final PasswordHasher passwordHasher;
    private final AuthProperties authProperties;
    private final Clock clock;
    private final SecureRandom secureRandom = new SecureRandom();

    public RefreshTokenService(
            UserRefreshTokenMapper tokenMapper,
            PasswordHasher passwordHasher,
            AuthProperties authProperties,
            Clock clock
    ) {
        this.tokenMapper = tokenMapper;
        this.passwordHasher = passwordHasher;
        this.authProperties = authProperties;
        this.clock = clock;
    }

    /** 签发新的 refresh token */
    public IssuedRefreshToken issueToken(Long userId) {
        LocalDateTime now = LocalDateTime.now(clock);
        String tokenId = UUID.randomUUID().toString().replace("-", "");
        String refreshToken = tokenId + TOKEN_SEPARATOR + newTokenSecret();
        LocalDateTime expiresAt = now.plusDays(authProperties.getRefreshTokenExpireDays());

        UserRefreshToken entity = new UserRefreshToken();
        entity.setUserId(userId);
        entity.setTokenId(tokenId);
        entity.setTokenHash(passwordHasher.hash(refreshToken));
        entity.setExpiresAt(expiresAt);
        entity.setCreatedAt(now);
        tokenMapper.insert(entity);

        return new IssuedRefreshToken(refreshToken, toRecord(entity));
    }

    /** 查找有效（未吊销且未过期）的 token 记录 */
    public Optional<RefreshTokenRecord> findActiveToken(String refreshToken) {
        Optional<ParsedRefreshToken> parsedToken = parseToken(refreshToken);
        if (parsedToken.isEmpty()) {
            return Optional.empty();
        }
        UserRefreshToken entity = tokenMapper.selectOne(
                new LambdaQueryWrapper<UserRefreshToken>()
                        .eq(UserRefreshToken::getTokenId, parsedToken.get().tokenId())
        );
        if (entity == null) {
            return Optional.empty();
        }
        RefreshTokenRecord record = toRecord(entity);
        if (!passwordHasher.matches(refreshToken, record.tokenHash())) {
            return Optional.empty();
        }
        if (!record.isActive(LocalDateTime.now(clock))) {
            return Optional.empty();
        }
        return Optional.of(record);
    }

    /** 吊销指定用户所有有效 token（登录时调用，实现单设备登录） */
    public void revokeActiveTokens(Long userId) {
        LocalDateTime now = LocalDateTime.now(clock);
        UserRefreshToken update = new UserRefreshToken();
        update.setRevokedAt(now);
        tokenMapper.update(update,
                new LambdaQueryWrapper<UserRefreshToken>()
                        .eq(UserRefreshToken::getUserId, userId)
                        .isNull(UserRefreshToken::getRevokedAt)
                        .gt(UserRefreshToken::getExpiresAt, now)
        );
    }

    /** 原子吊销单条 token（登出时调用），返回 true 表示成功吊销 */
    public boolean revokeToken(String refreshToken) {
        Optional<RefreshTokenRecord> activeToken = findActiveToken(refreshToken);
        if (activeToken.isEmpty()) {
            return false;
        }
        return revokeTokenById(activeToken.get().id());
    }

    /**
     * 原子吊销指定 ID 的 token：仅当 revoked_at 仍为 null 时才更新。
     * 返回 true 表示成功吊销，false 表示已被其他请求抢先吊销（疑似 token 窃取）。
     */
    public boolean revokeTokenById(Long id) {
        LocalDateTime now = LocalDateTime.now(clock);
        return tokenMapper.revokeByIdIfActive(id, now) > 0;
    }

    /** 解析 token 格式：tokenId.secret */
    private Optional<ParsedRefreshToken> parseToken(String refreshToken) {
        if (refreshToken == null || refreshToken.isBlank()) {
            return Optional.empty();
        }
        String[] segments = refreshToken.trim().split("\\Q" + TOKEN_SEPARATOR + "\\E", 2);
        if (segments.length != 2 || segments[0].isBlank() || segments[1].isBlank()) {
            return Optional.empty();
        }
        return Optional.of(new ParsedRefreshToken(segments[0], segments[1]));
    }

    /** 生成安全随机 secret */
    private String newTokenSecret() {
        byte[] bytes = new byte[SECRET_BYTES];
        secureRandom.nextBytes(bytes);
        return Base64.getUrlEncoder().withoutPadding().encodeToString(bytes);
    }

    private static RefreshTokenRecord toRecord(UserRefreshToken entity) {
        return new RefreshTokenRecord(
                entity.getId(),
                entity.getUserId(),
                entity.getTokenId(),
                entity.getTokenHash(),
                entity.getExpiresAt(),
                entity.getRevokedAt()
        );
    }

    private record ParsedRefreshToken(String tokenId, String secret) {
    }

    /** 签发后返回给调用方的 token 信息 */
    public record IssuedRefreshToken(String refreshToken, RefreshTokenRecord record) {
    }
}

几个值得注意的点，因为涉及到 Java/Spring 里几个常用套路：

Optional<T>：用来代替"可能返回 null"。findActiveToken 找不到有效 token 时返回 Optional.empty()，调用方必须显式处理"没有值"这个情况（用 .isEmpty()/.isPresent()/.get()），比直接返回 null 更不容易漏判导致空指针异常。
LambdaQueryWrapper：MyBatis-Plus 提供的查询构造器，UserRefreshToken::getTokenId 是方法引用，好处是类型安全、重构友好——如果你改了字段名，IDE 会自动帮你把这里也改掉；比手写 SQL 字符串（打错字段名要运行时才报错）安全得多。
revokeActiveTokens vs revokeTokenById 的区别：前者是"登录时把这个用户之前所有还活着的 token 全部作废"（实现单设备登录，新登录挤掉旧登录）；后者是"登出/刷新时只作废这一条"，而且用了原子更新（update ... where id = ? and revoked_at is null），如果两个请求同时想吊销同一条 token，只有一个能成功——这是并发场景下防止竞态条件的标准写法，revokeToken 返回 false 时甚至可以理解为"这个 token 已经被别人用过了，有被盗用嫌疑"。

写完后跑一下 ./gradlew compileJava 确认没有编译错误（应该没问题，字段名和你已有的 UserRefreshToken 实体、UserRefreshTokenMapper 完全对得上）。搞定后告诉我，下一步是 AuthService——把 PasswordHasher、JwtAccessTokenService、RefreshTokenService 三者串成完整的注册/登录业务逻辑。

我写好了，下一步做什么

继续之前得先说一句：AuthService 依赖了 User 模块里几个还没补全（或者路径不对）的东西，直接抄参考项目的 AuthService 会编译不过。所以这一步我们先把 user 模块的缺口补上，再回来写 AuthService——这正好也是你说的"包格式要严格对应"要处理的地方。

发现的问题

你现在的 User.java 放在 user.entity 包下，但参考项目是 user.model.entity（多一层 model）。这个多出来的 model 层是一种约定：把"数据结构"（model 下的 entity/dto/vo）和"逻辑代码"（service/controller/mapper）分开归类，模块大了之后好找文件。既然你要求严格对应，这里要挪一下。

另外 UserMapper 缺一个按用户名/邮箱查用户的方法，而且这个方法不能用简单注解写，得用 XML——这个我下面解释原因。

第一步：把 User 实体挪到正确的包

删除 user/entity/User.java，在 user/model/entity/User.java 新建同样内容（内容你已经写对了，只是位置不对）：

java
package com.example.myargus.user.model.entity;

import com.baomidou.mybatisplus.annotation.IdType;
import com.baomidou.mybatisplus.annotation.TableId;
import com.baomidou.mybatisplus.annotation.TableName;
import com.example.myargus.common.enums.SystemRole;
import com.example.myargus.common.enums.UserStatus;
import lombok.Data;

import java.time.LocalDateTime;

@TableName("users")
@Data
public class User {

    @TableId(type = IdType.AUTO)
    private Long id;
    private String userCode;
    private String username;
    private String email;
    private String displayName;
    private String passwordHash;
    private SystemRole systemRole;
    private UserStatus status;
    private Boolean mustChangePassword;
    private LocalDateTime lastLoginAt;
    private LocalDateTime createdAt;
    private LocalDateTime updatedAt;
}
第二步：UserMapper 加自定义查询方法
java
package com.example.myargus.user.mapper;

import com.example.myargus.user.model.entity.User;
import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import org.apache.ibatis.annotations.Mapper;
import org.apache.ibatis.annotations.Param;

import java.util.List;

@Mapper
public interface UserMapper extends BaseMapper<User> {

    /** 登录时根据用户名或邮箱查找用户，加行锁 */
    List<User> selectByLoginIdForUpdate(@Param("loginId") String loginId);
}

为什么这个方法要另外写 XML，而不是像 revokeByIdIfActive 那样用 @Update 注解？ 因为这条 SQL 需要 for update 行锁——登录时如果两个并发请求同时查同一个用户，加了行锁能防止竞态条件（比如同时触发"账号被禁用"之类的状态变更冲突）。这种带特殊语义、可能以后还要调整的查询，写成独立的 XML 文件比塞进注解字符串里更好维护，这也是 MyBatis 的两种映射方式并存的原因：简单 SQL 用注解，复杂/易变的 SQL 用 XML。

新建 src/main/resources/mappers/UserMapper.xml（这个目录还不存在，需要新建，你的 application.yaml 已经配置了 mapper-locations: classpath*:/mappers/**/*.xml 去扫描它，不用改配置）：

xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.myargus.user.mapper.UserMapper">

    <!-- 登录时根据用户名或邮箱查找用户，加行锁 -->
    <select id="selectByLoginIdForUpdate" resultType="com.example.myargus.user.model.entity.User">
        select id, user_code, username, email, display_name, password_hash,
               system_role, status, must_change_password
        from users
        where username = #{loginId} or email = #{loginId}
        order by id
        for update
    </select>

</mapper>

namespace 必须和 Mapper 接口的完整类路径一模一样，MyBatis 就是靠这个把 XML 里的 SQL 和接口方法对应起来的——写错了不会报编译错误，而是运行时找不到方法，调试起来比较烦，所以照抄时这里要格外小心。

第三步：新增 AdminUserItemResponse（VO）

放在 user/model/vo/AdminUserItemResponse.java：

java
package com.example.myargus.user.model.vo;

import com.example.myargus.common.enums.SystemRole;
import com.example.myargus.common.enums.UserStatus;

import java.time.LocalDateTime;

/**
 * 管理员用户列表中的单条用户信息。
 */
public record AdminUserItemResponse(
        Long userId,
        String userCode,
        String username,
        String email,
        String displayName,
        SystemRole systemRole,
        UserStatus status,
        boolean mustChangePassword,
        LocalDateTime lastLoginAt
) {
}

这是个 VO（View Object），专门给"返回给前端的数据"用，和 User 实体分开的原因是：User 里有 passwordHash 这种绝对不能返回给前端的字段，如果直接把实体序列化成 JSON 返回，一不小心就会把密码哈希也传出去。VO 只挑外部需要看到的字段，从设计上杜绝这类泄露。这个类现在虽然还用不上（要等做 user 模块管理员列表接口才用），但 UserQueryService 引用了它，所以先补上。

第四步：UserQueryService

放在 user/service/UserQueryService.java：

java
package com.example.myargus.user.service;

import com.example.myargus.common.exception.BusinessException;
import com.example.myargus.user.mapper.UserMapper;
import com.example.myargus.user.model.entity.User;
import com.example.myargus.user.model.vo.AdminUserItemResponse;
import com.baomidou.mybatisplus.core.conditions.query.LambdaQueryWrapper;
import org.springframework.stereotype.Service;

import java.util.List;

/**
 * 用户查询服务，提供对 users 表的通用查询。
 */
@Service
public class UserQueryService {

    private final UserMapper userMapper;

    public UserQueryService(UserMapper userMapper) {
        this.userMapper = userMapper;
    }

    /** 按 ID 升序返回所有用户 */
    public List<AdminUserItemResponse> listUsers() {
        List<User> users = userMapper.selectList(
                new LambdaQueryWrapper<User>().orderByAsc(User::getId)
        );
        return users.stream().map(UserQueryService::toAdminVo).toList();
    }

    /** 按 ID 查询单个用户，不存在则抛出异常 */
    public AdminUserItemResponse getUser(Long userId) {
        User user = userMapper.selectById(userId);
        if (user == null) {
            throw new BusinessException("用户不存在");
        }
        return toAdminVo(user);
    }

    /** 判断用户名是否已存在 */
    public boolean existsByUsername(String username) {
        return userMapper.exists(
                new LambdaQueryWrapper<User>().eq(User::getUsername, username)
        );
    }

    /** 判断邮箱是否已存在 */
    public boolean existsByEmail(String email) {
        return userMapper.exists(
                new LambdaQueryWrapper<User>().eq(User::getEmail, email)
        );
    }

    /** 按 ID 查询单个用户，返回精简 record（供跨模块调用），不存在返回 null */
    public UserRecord findById(Long userId) {
        User user = userMapper.selectById(userId);
        if (user == null) return null;
        return new UserRecord(user.getId(), user.getUserCode(),
                user.getDisplayName(), user.getSystemRole(),
                user.getStatus(), Boolean.TRUE.equals(user.getMustChangePassword()));
    }

    /** 供跨模块查询的用户精简信息 */
    public record UserRecord(
            Long userId,
            String userCode,
            String displayName,
            com.example.myargus.common.enums.SystemRole systemRole,
            com.example.myargus.common.enums.UserStatus status,
            boolean mustChangePassword
    ) {}

    /** 将 User 实体转为管理端 VO */
    private static AdminUserItemResponse toAdminVo(User user) {
        return new AdminUserItemResponse(
                user.getId(),
                user.getUserCode(),
                user.getUsername(),
                user.getEmail(),
                user.getDisplayName(),
                user.getSystemRole(),
                user.getStatus(),
                Boolean.TRUE.equals(user.getMustChangePassword()),
                user.getLastLoginAt()
        );
    }
}

这里为什么又单独定义一个 UserRecord，跟第三步的 AdminUserItemResponse 看着很像？因为用途不同：AdminUserItemResponse 是给"管理员用户列表接口"这种前端场景用的 VO；UserRecord 是给其他后端模块（比如接下来要写的 CurrentUserService）内部调用用的精简数据。虽然字段有重叠，但让不同层各自定义自己需要的数据结构，比让所有地方共用一个类更灵活——以后如果管理端要多显示一个字段，只改 VO，不会牵连到内部调用逻辑。

第五步：CurrentUserService

注意这个类不在 auth.service 或 auth.security 子包里，而是直接放在 auth 包的根下：auth/CurrentUserService.java。这是参考项目的设计——它是整个 auth 模块对外的"门面"，其它模块（document、qa 等以后要做的模块）想知道"当前是谁在请求"，只需要注入这一个类，不需要关心 JWT、UserContext 这些内部实现细节，所以特意放在包的最外层，表示这是模块的公开入口。

java
package com.example.myargus.auth;

import com.example.myargus.common.security.AuthenticatedUser;
import com.example.myargus.common.security.UserContext;
import com.example.myargus.common.enums.SystemRole;
import com.example.myargus.common.enums.UserStatus;
import com.example.myargus.common.exception.BusinessException;
import com.example.myargus.common.exception.ForbiddenException;
import com.example.myargus.common.exception.UnauthorizedException;
import com.example.myargus.user.service.UserQueryService;
import org.springframework.stereotype.Service;

/**
 * 当前用户服务，通过 {@link UserContext} 获取当前请求用户。
 * <p>
 * 依赖 JwtAuthenticationFilter 在请求进入时设置 {@link UserContext}。
 */
@Service
public class CurrentUserService {

    private final UserQueryService userQueryService;

    public CurrentUserService(UserQueryService userQueryService) {
        this.userQueryService = userQueryService;
    }

    /** 获取当前登录用户，未登录抛出 401 */
    public CurrentUser getRequiredCurrentUser() {
        AuthenticatedUser authenticatedUser = UserContext.get();
        if (authenticatedUser != null) {
            return loadUserById(authenticatedUser.userId());
        }
        throw new UnauthorizedException("当前请求未登录");
    }

    /** 要求当前用户为系统管理员，否则抛出 403 */
    public CurrentUser requireSystemAdmin() {
        CurrentUser currentUser = getRequiredCurrentUser();
        if (currentUser.systemRole() != SystemRole.ADMIN) {
            throw new ForbiddenException("当前用户不是系统管理员");
        }
        return currentUser;
    }

    /** 要求当前用户为业务用户（非管理员），否则抛出 403 */
    public CurrentUser requireBusinessUser() {
        CurrentUser currentUser = getRequiredCurrentUser();
        if (currentUser.systemRole() == SystemRole.ADMIN) {
            throw new ForbiddenException("系统管理员不能访问普通业务区");
        }
        return currentUser;
    }

    /** 从数据库加载用户并校验状态 */
    private CurrentUser loadUserById(Long userId) {
        UserQueryService.UserRecord user = userQueryService.findById(userId);
        if (user == null) {
            throw new BusinessException("当前用户不存在");
        }
        if (user.status() == UserStatus.DISABLED) {
            throw new BusinessException("账号已被禁用");
        }
        return new CurrentUser(
                user.userId(),
                user.userCode(),
                user.displayName(),
                user.systemRole(),
                user.mustChangePassword()
        );
    }

    /** 当前登录用户信息 */
    public record CurrentUser(
            Long userId,
            String userCode,
            String displayName,
            SystemRole systemRole,
            boolean mustChangePassword
    ) {
        public CurrentUser(Long userId, String userCode, String displayName) {
            this(userId, userCode, displayName, SystemRole.USER, false);
        }
    }
}

为什么明明 UserContext 已经存了 AuthenticatedUser（里面已经有 userId、displayName 等信息），这里还要 loadUserById 重新查一次数据库？ 因为 JWT 里的信息是登录那一刻的快照，是不实时的。如果用户在 token 有效期内被管理员禁用了账号，UserContext 里的 AuthenticatedUser 还是"启用"状态（因为 token 还没过期），但数据库里已经是 DISABLED 了。所以每次真正需要判断权限时，都重新查一次库，保证拿到的是最新状态——这是用一点点性能换安全性的常见取舍。

这五步做完，user 模块和 CurrentUserService 就补齐了。你可以先跑一下 ./gradlew compileJava 确认这五个文件互相之间没有拼写/导入错误，跑通了告诉我，我们再回头写真正的 AuthService（登录、注册、刷新令牌、登出的完整业务逻辑）。